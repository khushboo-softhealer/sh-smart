<?xml version="1.0" encoding="UTF-8"?>
<odoo>
   <template id="theme_softhealer_store_pricelist_selector" name="Custom Pricelist Selector">
      <t t-nocache="The query strings can change for the same page and the same rendering." t-nocache-_classes="_classes" t-nocache-_dropdown_classes="_dropdown_classes" t-nocache-_is_bottombar="_is_bottombar">
         <t t-set="website_sale_pricelists" t-value="website.get_pricelist_available(show_visible=True)" />
         <!--<t t-if="website_sale_order and website_sale_order.pricelist_id" t-set="curr_pl" t-value="website_sale_order.pricelist_id"/>-->
         <t t-if="website and website.pricelist_id" t-set="curr_pl" t-value="website.pricelist_id"/>
         
         <div t-attf-class="o_pricelist_dropdown dropdown#{'' if website_sale_pricelists and len(website_sale_pricelists)&gt;1 else ' d-none'}">
            <a role="button" href="#" class="dropdown-toggle btn" data-bs-toggle="dropdown">
               <t t-esc="curr_pl and curr_pl.currency_id and curr_pl.currency_id.symbol or ' - '" /> <span t-esc='curr_pl.currency_id.name'></span>
            </a>
            <div class="dropdown-menu" role="menu">
               <t t-foreach="website_sale_pricelists" t-as="pl">
                  <a role="menuitem" t-att-href="'/shop/change_pricelist/%s' % pl.id" class="dropdown-item">
                     <span class="switcher_pricelist" t-att-data-pl_id="pl.id" t-esc="pl.currency_id.symbol" /> <span t-esc='pl.currency_id.name'></span>
                  </a>
               </t>
            </div>
         </div>
      </t>
   </template>
</odoo>