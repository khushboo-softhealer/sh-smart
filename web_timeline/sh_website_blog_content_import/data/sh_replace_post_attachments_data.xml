<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Multi Action (Replace Attachments) -->
    <record id="sh_replcae_post_attachments" model="ir.actions.server" >
        <field name="name">Replace Post Attachments</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="model_blog_post" />
        <field name="binding_model_id" ref="model_blog_post" />
        <field name="state">code</field>
        <field name="code">
            action = records.replace_post_attachments()
        </field>
    </record>
</odoo>