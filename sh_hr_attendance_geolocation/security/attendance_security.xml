<?xml version="1.0"?>
<odoo>        

    <record id="hr_attendance.hr_attendance_rule_attendance_manager" model="ir.rule">
        <field name="name">attendance officer: Own + Under Employee</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="domain_force">['|',('employee_id.user_id','=',user.id),('employee_id.coach_id.user_id','=',user.id)]</field>
        <field name="groups" eval="[(4,ref('hr_attendance.group_hr_attendance_user'))]"/>
    </record>

    <record id="hr_attendance_rule_attendance_super_manager" model="ir.rule">
        <field name="name">attendance Manager: full access</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4,ref('hr_attendance.group_hr_attendance_manager'))]"/>
    </record>

    <record id="group_access_extra_allow_check_in" model="res.groups">
            <field name="name">Access Allow Extra Check-In</field>
        </record>
</odoo>
