<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sh_account_payment_view_search_inherited" model="ir.ui.view">
        <field name="name">sh_account_payment_inherit_view_search</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_search" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='reconciled']" position="after">
                <separator />
                <!-- payment_date(v12) -> date(v16) -->
                <filter name="last_month" string="Last Month" domain="[('date','&gt;=',(context_today()-relativedelta(months=1)).strftime('%Y-%m-01')),('date','&lt;',time.strftime('%Y-%m-01'))]" />
                <filter name="last_week" string="Last Week" domain="[('date','&gt;=',(context_today()+relativedelta(weeks=-2,days=1,weekday=-1)).strftime('%Y-%m-%d')),('date','&lt;',(context_today()+relativedelta(weeks=-1,days=1,weekday=6)).strftime('%Y-%m-%d'))]" />
                <filter name="last_year" string="Last Year" domain="[('date', '&gt;=' ,(context_today()-relativedelta(years=1)).strftime('%Y-01-01')),('date','&lt;',time.strftime('%Y-01-01'))]" />
                <filter name="this_month" string="This Month" domain="[('date','&lt;',(context_today()+relativedelta(months=1)).strftime('%Y-%m-01')), ('date','&gt;=',time.strftime('%Y-%m-01'))]" />
                <filter name="this_week" string="This Week" domain="[('date','&gt;=',(context_today()+relativedelta(weeks=-1,days=1,weekday=-1)).strftime('%Y-%m-%d')),('date','&lt;',(context_today()+relativedelta(weekday=6)).strftime('%Y-%m-%d'))]" />
                <filter name="this_year" string="This Year" domain="[('date','&lt;=', time.strftime('%Y-12-31')),('date','>=',time.strftime('%Y-01-01'))]" />
                <filter name="yesterday" string="Yesterday" domain="[('date','>=', ((context_today()-datetime.timedelta(days=1)).strftime('%Y-%m-%d'))), ('date','&lt;', ((context_today()+datetime.timedelta(days=0)).strftime('%Y-%m-%d')))]" />
                <filter name="today" string="Today" domain="[('date', '=', context_today().strftime('%Y-%m-%d'))]" />
            </xpath>
        </field>
    </record>
</odoo>
